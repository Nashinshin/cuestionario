{
  "name": "cuestionario",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.0.0-rc.0"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /cuestionario",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "gh-pages": "^6.1.1"
  }
}
